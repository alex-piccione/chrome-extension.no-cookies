{
  "scripts": {
    "build:tsc-only": "npx tsc",
    "build": "parcel build scripts/content.ts --dist-dir dist/scripts --no-source-maps"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.317",
    "parcel": "^2.14.4",
    "typescript": "^5.8.3"
  }
}
